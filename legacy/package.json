{
  "name": "spinforge",
  "version": "0.1.0",
  "description": "Spinforge — Mystic roulette roguelike",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "vite --open",
    "dev": "vite",
    "build": "vite build",
    "build:all-palettes": "node scripts/build-palettes.js && node scripts/build-index.js",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "nes.css": "^2.2.1"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vitest": "^3.1.1"
  }
}
